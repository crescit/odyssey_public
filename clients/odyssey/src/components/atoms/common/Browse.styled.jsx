import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    root: {
      maxWidth: 425,
    },
    media: {
      height: 140,
    },
    palette: {
      primary: {
        main: '#212121',
      },
    },
  });
