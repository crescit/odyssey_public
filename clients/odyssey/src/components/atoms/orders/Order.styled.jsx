import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
        marginTop: 0,
        marginBottom: 0,
        textAlign: 'left',
    },
    availableDateTime: {
        textAlign: 'left',
        marginBottom: 15,
        marginLeft: 10,
    }
}));  